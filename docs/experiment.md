# Overfit harness (`overfit_wgpu`) experiments

For a **self-contained handoff** (goals, commands, file map, conventions), see [`overfit_wgpu.md`](overfit_wgpu.md).

All runs use **burn-wgpu** with the FusedLSTM / CubeCL path, default `lr=1e-2`, `seq_len=300`, `batch_size=32` unless stated otherwise. Pass criteria: **T1** final RMSE < 50 MMR, **T2** final RMSE < 300 MMR, **T3** per-rank **SSL** RMSE < 350 MMR.

**Loss paths:** **Default harness** (no `--mse-only`) uses **`production_training_minibatch_loss`** — Huber, τ-averaged pinball, rank weights, ordinal, pairwise, minibatch spread. **`--mse-only`** uses **`mse_ablation_minibatch_loss`**: MSE on jittered targets, **the same masked minibatch spread term** (separate weight), **no** Huber / pinball / ordinal / pairwise; unit rank weights unless you add T3 row boost.

| # | Date (approx) | Description | T1 (final RMSE) | T2 (final RMSE) | T3 (overall RMSE) | T3 (SSL RMSE) | Pass |
|---|----------------|------------|-----------------|-----------------|-------------------|----------------|------|
| 1 | 2026-04-25 | **Baseline harness:** sequential indices `(0..n)`, vanishing MSE on normalised targets, `forward` only, no rank weights / pinball / oversampling. | 22.9 | 791.3 | 550.1 | 1367.7 | T1 only |
| 2 | 2026-04-25 | **Phase A + B (harness only):** `build_oversampled_indices` per epoch, pinball (same constants as `training.rs`), inverse-frequency `compute_inverse_frequency_weights` + `lookup_rank_weights`, `forward_with_lobby_scale` alternating 1.0 / 0.0 per batch, stretched warmup and cosine `lr` floor 0.05, collapse-diag logging. *No* `LOBBY_BIAS_OUTPUT_SCALE` yet; *i.i.d.* label jitter not in this row (see 3). | 33.7 (early stop) | 1284.9 | 553.6 | 1297.2 | T1 only |
| 3 | 2026-04-25 | **Phase C:** `LOBBY_BIAS_OUTPUT_SCALE = 0.2` on lobby bias in `SequenceModel` (`ml_model`), **mean-zero** label jitter in `training.rs` and the same in `overfit_wgpu` (loss on jittered targets, RMSE on clean targets). | 25.3 (early stop) | 1285.0 | 554.0 | *not in log snippet* | T1 only |
| 4 | 2026-04-25 | **`--mse-only`:** MSE on jittered targets, **no** pinball, **unit** rank weights; same oversampling / lobby alternation / schedule / mean-zero jitter / `LOBBY_BIAS_OUTPUT_SCALE`. | 2.1 (early stop) | 1033.0 | 548.8 | 1377.7 | T1 only |
| 5 | 2026-04-25 | **More steps + higher LR:** `--mse-only --lr 3e-2 --t2-epochs 200 --t3-epochs 250` (put `--epochs` before per-tier overrides if you use both). | 44.2 (early stop) | 320.3 | 549.1 | 1388.4 | T1 only |
| 6 | 2026-04-25 | **T2 pass recipe:** `--mse-only --lr 3e-2 --t2-epochs 270 --t3-epochs 250 --lr-floor 0.10`. T2 early-stops ~139 when RMSE &lt; 300. | 23.6 (early stop) | 96.0 (early stop) | 432.5 | 1128.5 | T1+T2 |
| 7 | 2026-04-26 | **T1+T2+T3 pass** (harness + model): `LOBBY_BIAS_OUTPUT_SCALE = 0`, `LABEL_JITTER_STD = 40` (harness + `training.rs`), `Wgpu::<f32>::seed(&device, 42)` for reproducibility, T3-only MSE row boost (mean target &gt; 1800 MMR → 10× weight), T3 uses `1.5×` base LR and `max(0.15, --lr-floor)` cosine floor. CLI: `--mse-only --lr 3e-2 --t2-epochs 270 --t3-epochs 450 --lr-floor 0.10`. | 27.9 (early stop) | 184.9 (early stop) | 228.4 | 230.4 | **All pass** |
| 8 | 2026-04-26 | **Production parity refactor:** `minibatch_loss.rs` — `train()` and the default harness call `production_training_minibatch_loss` (Huber, pinball, rank weights, ordinal, pairwise). **`--mse-only`** uses `mse_ablation_minibatch_loss`: `forward_with_lobby_scale` (same MMR path as eval), shared `mean_zero_label_jitter_normalized`, plain MSE (no `0.5` factor), T3 row boost unchanged. Same CLI as row 7. | 17.6 (early stop) | 147.3 (early stop) | 327.2 | 210.5 | **All pass** |
| 9 | 2026-05-01 | **Intermittent T3 failure, same recipe as rows 7–8:** Code still has row-8 `mse_ablation` (lobby forward, shared jitter, plain `diff²`, T3 row boost, `LOBBY_BIAS_OUTPUT_SCALE = 0`, `Wgpu` seed 42). **T1+T2 pass**; **T3** falls into **constant-MMR** basin (per-rank RMSE ~64 MMR steps, SSL ≫ 350). Example CLI: `--mse-only --lr 3e-2 --t2-epochs 270 --t3-epochs 500 --lr-floor 0.10` (500 T3 epochs did not fix SSL). Other re-runs on the same tree saw SSL ~940–980 with 450 epochs. | 17.6 (early stop) | 147.3 (early stop) | 670.1 | 1013.0 | T1+T2 only |
| 10 | 2026-04-26 | **Production loss + numerics (`minibatch_loss.rs`):** τ-averaged pinball on `{0.1, 0.25, 0.75, 0.9}` (high-rank mask) + weaker full-batch pinball; **minibatch spread** (masked std of clean targets vs predictions, normalised space) in production and in **`mse_ablation`**; ordinal **BCE via `log_sigmoid`** + logits clamp; spread variance floor **`1e-5`** (was `1e-8`) to tame `sqrt` autodiff; ordinal tensors **`[flat_row_count, 1]`** before `expand` (fixes `[1, 96]` vs `[96, 21]` on Burn 0.20); label jitter from `Tensor::<B, 1>::from_floats` then `reshape`. **Default harness** (production loss). CLI: `--lr 3e-2 --t2-epochs 270 --t3-epochs 500 --lr-floor 0.10` (no `--mse-only`). | 19.0 (early stop) | 93.6 (early stop) | 167.1 | 310.0 | **All pass** |
| 11 | 2026-05-17 | **Full-pipeline mean-collapse triage:** harness T1+T2+T3 still pass on row-10 code, but real training (~78 epochs, 378k segments) settles into a constant-MMR basin around diamond-1 (per-rank validation U-shape: bronze-1 796 / diamond-1 41 / SSL 1308 MMR RMSE). Disabled the two production-only knobs that the harness lacks: `SMURF_MASK_START_EPOCH: Option<usize> = None` in `crates/ml_model_training/src/training.rs` (was `Some(5)`; top-1% EMA mask preferentially drops tail segments and reinforces the collapse) and `ModelConfig::dropout` default `0.0` in `crates/ml_model/src/lib.rs` (was `0.2`; harness already runs with dropout 0). Also changed the per-rank validation log so `n` reports the **predicted-rank histogram** rather than the label count (`crates/ml_model_training/src/training.rs::PerRankErrors`: `predicted_count` / `correct_count`), making mean collapse trivially visible in the training output. Harness CLI unchanged. | n/a (no harness re-run yet) | n/a | n/a | n/a | n/a (pending full re-train) |
| 12 | 2026-05-17 | **Production LR aligned with the harness:** row-11 re-run hit the basin at epoch 1 and was still there at epoch 18 (every prediction → diamond-1) because production was running with `LEARNING_RATE=0.001` while the harness uses `lr=3e-2` (30× higher). Bumped `examples/pipeline.rs` `LEARNING_RATE` default to `0.03`, `FullTrainConfig::default().learning_rate` to `0.03`, and `TrainingConfig::learning_rate` `#[config(default)]` to `3e-2`. Rewrote `training::cosine_lr` to mirror the harness schedule: linear warmup over `min(epochs/4, 20)` epochs from `0.10×base` up to `1.0×base`, then cosine down to a `0.10×base` tail floor (constants `COSINE_LR_WARMUP_FRACTION`, `COSINE_LR_WARMUP_MAX_EPOCHS`, `COSINE_LR_WARMUP_FLOOR`, `COSINE_LR_TAIL_FLOOR`). Warmup re-applies on checkpoint resume which is intentional (gentle re-engage of Adam state). | n/a | n/a | n/a | n/a | n/a (pending full re-train) |
| 13 | 2026-05-18 | **Row-12 re-run still in the basin at epoch 27** (loss 0.0369 ≈ 480 MMR; every prediction → diamond-1). Diagnosis: with predicted ≈ 902 MMR for every slot, the Huber up-pull from SSL targets and down-pull from Bronze-1 targets cancel near the population mean, making diamond-1 a stable saddle that pinball alone (weight 0.3, masked above 1400 MMR) can't dislodge. Three targeted changes in `crates/ml_model_training/src/minibatch_loss.rs` and `crates/ml_model_training/src/training.rs`: (1) **per-element rank weights** — `lookup_rank_weights_slice` now feeds in each slot's own raw target MMR instead of the lobby-mean MMR, so an SSL player in a mixed lobby keeps SSL's inverse-frequency weight; (2) **symmetric tail row boost** in `production_training_minibatch_loss` — rows whose clean mean target is above `TAIL_BOOST_HIGH_THRESHOLD_MMR=1800` **or** below `TAIL_BOOST_LOW_THRESHOLD_MMR=250` get every element multiplied by `TAIL_BOOST_MULTIPLIER=10.0`, mirroring the harness `MseExtremeMmrRowBoost` that lifted SSL in T3 (rows 7/8/10); (3) **ordinal-head diagnostic** in the validation log — `PerRankErrors` gained `ordinal_predicted_count` / `ordinal_correct_count` derived from `forward_with_ordinal_scale`'s 21 boundary logits (`count(logit > 0)` mapped to a `Rank` via `Rank::all_ranked()`), and the log now shows `reg(p,v) ord(p,v)` per rank. If the ordinal histogram differentiates ranks while the regression histogram stays collapsed, the LSTM is learning but the regression head is the bottleneck. | n/a | n/a | n/a | n/a | n/a (pending full re-train) |
| 16 | 2026-06-13 | **Signal probe confirms rank signal; first re-train escapes basin then NaN crash.** (a) **Signal probe** (`signal_probe`): ridge regression on 8 aggregate features over 378k segments → val RMSE 487 MMR vs 557 constant baseline (+12.6 %), Pearson r = 0.485. Verdict: features carry rank signal, problem is optimisation. (b) **Full-pipeline re-train** (row-15 bug fixes active): model escapes collapse, reaches **pred_std = 410 MMR, pearson_r = 0.825** by epoch 45 — per-rank RMSE drops to 200–250 MMR in the mid-ladder (gold–diamond). Tails still high (bronze-1 400, SSL 801). At epoch 46 batch 712, cubecl `Task failed: Any { .. }` (GPU kernel crash) → next forward reads garbage → **NaN → Adam poisoned → NaN forever**. (c) **NaN kill-switch added** (`training.rs`): every batch checks `loss.is_nan() \|\| loss.is_infinite()` before backward; on NaN, aborts the epoch immediately, reloads the last saved checkpoint, resets Adam, and retries the epoch. Gives up after `NAN_MAX_RECOVERY_ATTEMPTS = 3`. The `last_good_checkpoint_path` is updated whenever a checkpoint save succeeds. | n/a | n/a | n/a | n/a | NaN at epoch 46 batch 712 (pending re-run) |
| 15 | 2026-06-09 | **Bug-fix pass + diagnostics (no re-train yet):** Two unit bugs silently disabled the auxiliary anti-collapse losses. (1) **Ordinal double-scale** (`minibatch_loss.rs`): `raw_targets * MMR_SCALE` inflated every target ~2500× before comparing against `ORDINAL_BOUNDARIES_MMR`, forcing all 21 ordinal targets to 1 (everyone = SSL) and wasting ~20 % of loss weight. Fixed: drop the multiply. Regression test added. (2) **Pairwise hinge unit mismatch** (`minibatch_loss.rs`): `pred_diff` was raw MMR but `pairwise_hinge_margin = 50/MMR_SCALE = 0.02`; any 0.02 raw-MMR difference satisfied the hinge, giving zero ranking gradient. Fixed: normalise predictions before `pred_diff`. Regression test added. (3) **Basin-escape gate** (`full_pipeline.rs`): the `mean_rank_RMSE / √2` heuristic returns ~270 MMR for a fully collapsed model (above the 100 MMR guard), so the gate never fired. Fixed: thread real `pred_std_mmr` from `CollapseAccumulator` through `ValidationLossResult.pred_std_mmr` → `TrainingState.last_validation_pred_std_mmr` → `TrainingOutput.final_validation_pred_std_mmr`. (4) **Collapse diagnostics** (`training.rs`): `CollapseAccumulator` logs pred_std, Pearson r, constant-predictor RMSE, lobby-mean RMSE after every validation pass; `warn!` fires if pred_std < 50 MMR. (5) **Warm-start batch-size** (`full_pipeline.rs`): `WARM_START_BATCH_SIZE=32` yields ~7 steps/epoch × 250 epochs ≈ 1 750 steps vs ~500 at production batch 144. (6) **`signal_probe` binary** (`ml_model_training/src/bin/signal_probe.rs`): ridge regression on 8 per-player aggregate features to confirm rank signal before GPU runs. | n/a | n/a | n/a | n/a | n/a (pending re-train) |
| 14 | 2026-05-24 | **Row-13 still collapsed — regression head → platinum-3, ordinal head → SSL constantly — so the LSTM itself is not differentiating per-player features.** Reverted the row-13 symmetric tail boost (`TAIL_BOOST_*` constants removed from `production_training_minibatch_loss`); per-element rank weights and the ordinal diagnostic stay — they're useful regardless. Added a **warm-start phase** in `full_pipeline::run_with_config` (`crates/rocket_league_score/src/commands/full_pipeline.rs`): before the main `train()` call, build a balanced `WARM_START_SEGMENTS_PER_RANK=10` × 23-rank mini-set from the existing on-disk segment cache via the new `SegmentStore::subset_by_indices` and the new `ml_model_training::balanced_segment_indices` helper, then run `train()` on that subset for `WARM_START_EPOCHS=250` with the same loss / optimizer / LR schedule as main training (same warmup + 0.10 cosine floor, same `production_training_minibatch_loss`, same per-element rank weights). The model weights carry into main training in-place. Warm-start is skipped automatically on checkpoint resume (`start_state.is_some()`). Also added a **tail-rank guard** (`TrainingState::tail_rank_baseline`, populated from `TrainingOutput::final_validation_rank_rmse` at the end of warm-start): each main-training validation pass compares bronze-1 / grand-champion-3 / supersonic-legend RMSE to the warm-start baseline and `warn!`s if any rank degrades by more than `TAIL_RANK_GUARD_DEGRADATION_FRACTION=0.25` (25 %). Monitoring only — no automatic rollback. Defaults are baked into code constants, not config / env vars, so the user-visible CLI is unchanged. | n/a | n/a | n/a | n/a | n/a (pending full re-train) |
| 15 | 2026-06-24 | **Within-lobby smurf spot-check on REAL coherent lobbies — current model does NOT discriminate the outlier.** New `database::list_mixed_rank_replays(min_top_gap_mmr)` (`crates/database/src/replay.rs`, coarse-rank canonical MMR mapped in SQL) + new `crates/rocket_league_score/examples/smurf_spotcheck.rs` (NdArray/CPU; loads a checkpoint, runs `predict_player_centric_per_segment` over each whole replay, per-player = median across segments, applies the production `is_this_a_smurf` rule `pred > lobby_median(preds) + 200`). Scored `lstm_v20` `checkpoint_best` / `epoch50` / `epoch500` over 74 real lobbies (`min-top-gap 200`, ~SSL-among-GC dominated). Result: detection ≤1.4 %, **top-1 13–18 % (= chance, 1/6≈16.7 %)**, **mean margin ≈ 0 MMR** — the model predicts the known high player essentially identically to their lobbymates. epoch500 (more absolute-rank spread) was no better at within-lobby discrimination than epoch50. Confirms the smurf signal is not yet recoverable: model leans on lobby/opponent context (or cannot resolve the label-saturated SSL-vs-GC ceiling). The candidate pool is also almost entirely top-of-ladder; mid-ladder big-delta (party) lobbies are nearly absent — motivates widened Ballchasing fetch (`api/client.rs` currently bands `min-rank`…`min-rank+4`) and/or within-game relative features. | n/a | n/a | n/a | n/a | n/a (diagnostic) |
| 16 | 2026-06-24 | **Calibration scan — mid-ladder big-delta lobbies are abundant; the old scarcity was a collection artifact.** Added `BallchasingClient::list_replays_in_band` / `fetch_replay_list_page` (+ `pub next_replay_list_url_with_max_rank`) in `crates/ballchasing_downloader/src/api/client.rs` and `crates/ballchasing_downloader/examples/scan_mixed_rank.rs`, which pages the **list** endpoint (per-player ranks are in the summary, so no replay downloads needed) over a wide band and tallies within-lobby `top_gap = max − median` from coarse-rank MMR. Scan of **20,000** ranked-standard `gold-1..champion-3` summaries (16,755 with ≥3 known ranks): **mid-ladder gap≥150 = 415 (2.08 % yield)**, gap≥100 = 2,074 (10.4 %), gap≥200 = 129 (0.65 %). vs only **27** gap≥150 mid-ladder lobbies in the current 27.7k-replay DB — confirming the homogeneity was caused by the `min-rank..min-rank+4` narrow-band ingestion, not by ranked itself. Projected: 2,500 gap≥150 lobbies ≈ 120k summaries ≈ ~10 min of list scanning. **Binding constraint is the file-download quota (200/hr ⇒ ~13 h for 2,500), not availability.** gap≥200 is comparatively rare (~32 h to download 2,500) so target gap≥150. | n/a | n/a | n/a | n/a | n/a (calibration) |
| 19 | 2026-06-24 | **Smart collapse-aware cutoff (spread/correlation-gated early stop).** `crates/ml_model_training/src/training.rs`: new module consts `COLLAPSE_CUTOFF_GRACE_EPOCHS=80`, `COLLAPSE_CUTOFF_PATIENCE=40`, `COLLAPSE_CUTOFF_PRED_STD_MMR=150`, `COLLAPSE_CUTOFF_MAX_PEARSON_R=0.30`, `COLLAPSE_PROGRESS_MIN_STD_GAIN_MMR=5`, `COLLAPSE_PROGRESS_MIN_PEARSON_GAIN=0.01`. Each validation pass, "progress" = a rise in **prediction spread** (`pred_std_mmr`) **OR** **rank correlation** (`pearson_r`) vs the high-water mark at the last reset — this is the "more high/low-end predictions" signal that shows the model is still improving even mid-overfit, and it resets the patience counter (so a slow, steady widening is never cut). The cutoff fires only when ALL hold: `epoch ≥ 80`, no progress for `40` passes, **and** still collapsed (`pred_std < 150` AND `pearson_r < 0.30`) — a model that's spreading or ordering well is immune. Plumbing: `pearson_r` added to `ValidationLossResult`; `TrainingState` gains `best_pred_std_mmr` / `best_pearson_r` / `epochs_without_collapse_progress` / `collapse_cutoff_enabled`. Armed for the **main** run only (`full_pipeline.rs` sets `collapse_cutoff_enabled=true`); warm-start and ad-hoc callers leave it off so the escape phase is never truncated. On fire it saves `_final` and returns like the existing loss-based stop. | n/a | n/a | n/a | n/a | n/a (mechanism) |
| 18 | 2026-06-24 | **Training-side smurf prep: mixed-lobby oversampling + held-out smurf eval.** `crates/ml_model_training/src/segment_cache.rs`: `build_oversampled_indices` now multiplies each segment's rare-rank repeat factor by `MIXED_OVERSAMPLE_FACTOR=5` when the segment's within-lobby top-gap is ≥ `MIXED_TOP_GAP_THRESHOLD_MMR=150` (new `segment_top_gap_mmr` helper = `max(known) − median(known)` over `target_mmr`, unknowns = `0.0` excluded; mirrors the fetch-pipeline acquisition metric so the 2,412 fetched mid-ladder lobbies are exactly the ones boosted). Applied per-epoch on the **training** `SegmentStore` only (`training.rs` line 392), so the boost never touches validation; `debug!` logs `mixed_segment_count` / `total_indices`. **Held-out smurf eval needs no new code:** `assign_dataset_splits` already routes ~`1−train_ratio` of the new mixed lobbies into the persisted **Evaluation** split, and `smurf_spotcheck --split evaluation` reports detection/top-1/margin on exactly those held-out lobbies (oversampling can't leak them into training). Constants are baked in code (no CLI/env change). Unit tests: `test_segment_top_gap_mmr`, `test_mixed_lobby_oversampling`. **Resume guidance:** start a **fresh** run — epoch-500 weights sit in an overfit homogeneous basin at the LR floor and (row 15) are no better at within-lobby discrimination than epoch 50; the augmented data + held-out split also make from-scratch comparisons clean. | n/a | n/a | n/a | n/a | n/a (pending re-train on fetched data) |
| 17 | 2026-06-24 | **Mixed-rank fetch pipeline (target 2,500 mid-ladder gap≥150).** New `database::list_pending_mixed_rank_replays(min_gap, limit)` (same coarse-MMR metric as `list_mixed_rank_replays`, status `not_downloaded`), new lib module `ballchasing_downloader::mixed_rank_fetch` (`MixedRankFetchConfig` + `run`) and binary `src/bin/fetch_mixed_rank.rs`. **Discovery:** pages the list endpoint over `ranked-standard [gold-1..champion-3]`, computes `top_gap = max − median` over rank-known players (coarse canonical MMR, reusing `bundle::extract_players_with_average_rank` for both `folder_rank` and ready-to-insert `ReplayPlayer`s), dedups against `list_all_replay_ids`, and batch-inserts replays + players until `target_new_replays` new lobbies are stored. **Download:** resumable loop over `list_pending_mixed_rank_replays`, honoring the 200/hr file quota + 429 `Retry-After`. Run: `cargo run --release --bin fetch_mixed_rank -- --target 2500 --min-gap 150 [--no-download]`. Smoke test (target 3) inserted 3 lobbies with coarse gaps 310/196/161 — fetch filter and DB selector agree exactly. These replays are identifiable for training/eval via `list_mixed_rank_replays(150)` once downloaded + segment-cached. | n/a | n/a | n/a | n/a | n/a (data acq) |
| 20 | 2026-08-23 | **Whole-match + within-lobby validation metrics (measurement change, no training change).** Every metric up to this row scored one ~20-second segment slot; production does not (`predict_player_centric` averages a player over all a replay's segments, `is_this_a_smurf` takes the median, then compares against the **lobby median**). Root-cause reading of the epoch-255 log: `lobby_mean_baseline=59.3` vs `constant_baseline=470.8` means **only 1.6 % of label variance is within-lobby** — the objective is ~98 % lobby identification, and since every player's 106 features contain the other five cars, predicting the lobby six times is the correct answer to the question being asked. Changes: `segment_cache.rs` — `SegmentEntry` gains `replay_id` (populated from `SegmentFileInfo`, carried through `subset_by_indices`), new `SegmentStore::get_replay_id`, `segment_top_gap_mmr` and `MIXED_TOP_GAP_THRESHOLD_MMR` made `pub`. `training.rs` — new `WithinLobbyAccumulator` regroups validation predictions by replay and reports, per validation pass: **per-player-per-match RMSE** (median over that player's segments), **per-lobby RMSE**, a **between/within decomposition** of the player error, **within-lobby pairwise concordance** (0.500 = chance, pairs need a >25 MMR true gap since adjacent divisions sit ~19 apart), and on held-out **mixed lobbies** (gap ≥ 150, same threshold as the fetch pipeline) **top-1 / detection / mean margin** using the shipped `+200 MMR over lobby median` rule. Top-1 gives **fractional credit on prediction ties**, so a collapsed model scores exactly chance (1/6) instead of 100 %. Exposed as `ValidationLossResult::within_lobby` → `TrainingState::last_validation_within_lobby` so stopping rules and checkpoint selection can later move onto it. 5 unit tests: perfect ordering, collapsed-model-scores-chance, between/within separation, segment-median robustness, homogeneous/unknown-slot exclusion. `lobby_mean_baseline` deliberately left on the **mean** so the number stays comparable with rows 11–19; the new metrics use the median where robustness to a smurf matters. **Note:** the `200.0` threshold is now duplicated in three places (`training.rs`, `is_this_a_smurf::prediction`, `smurf_spotcheck`) and should be unified. | n/a | n/a | n/a | n/a | n/a (measurement; re-score epoch-255 checkpoint) |
| 21 | 2026-08-23 | **Shipped smurf rule unified + `smurf_spotcheck` top-1 tie bug fixed (eval integrity, no training change).** (a) **One threshold, three call sites:** the `200.0` MMR flag margin existed as three independent literals (`is_this_a_smurf::prediction::SMURF_SUSPICION_MMR_ABOVE_LOBBY_MEDIAN`, `smurf_spotcheck::SMURF_THRESHOLD_MMR`, row-20's `WITHIN_LOBBY_SMURF_MARGIN_MMR`), so the app's verdict, the offline spot-check, and the in-training metric could silently disagree about what "detected" means. Now one definition, `ml_model::SMURF_MARGIN_OVER_LOBBY_MEDIAN_MMR`; the other three alias it and keep their local names. (b) **Top-1 tie bug:** `slot_preds.iter().max_by(...)` returns the **last** maximum, so under a collapsed model — every slot identical — `argmax_slot` was always slot 5 and `top1` reduced to `high_slot == 5`, i.e. "how often is the outlier the alphabetically-last orange player". Not chance, and biased by roster alphabetisation. Replaced with **fractional credit** (`1/k` for a k-way tie), so a fully collapsed model now scores exactly 1/6; per-row output gained a `tie` state and the summary prints the `(chance 16.7%)` baseline inline. **This means row 15's 13–18 % top-1 was not measured the way it was reported and should be re-run before being cited.** (c) **Slot mapping hardened:** `high_slot` was located in `parsed.frames.first().players` order. That happens to agree with the model's canonical roster — the parser sorts each frame by `(team, name)` and `Team::Blue < Team::Orange` — but only when all six players are present in frame 0. Now uses `PlayerRoster::from_frames`, the same roster `predict_player_centric_per_segment` builds, so a late spawn cannot shift `slot_preds` out from under `high_slot`. | n/a | n/a | n/a | n/a | n/a (eval fix; re-run row 15) |
| 22 | 2026-08-25 | **Trajectory-mined smurf labels: NO-GO — the candidate set is mostly display-name collisions.** Ran `scripts/smurf-trajectory-sizing.sql` (step 0 of the handoff; previously blocked, `DATABASE_URL` + `psql` are available on this machine). (a) **Recurrence:** 108,011 distinct `player_name`s over 30,163 downloaded replays; 20,304 seen ≥2×, 4,303 ≥5×, 1,608 ≥10×, mean 1.67 appearances, max 573. (b) **Climbs:** 3,849 players with ≥3 dated matches; 1,292 gained ≥4 divisions, 482 ≥8, **92 ≥8 within 30 days, 39 ≥12 within 30 days**. (c) **The 39 candidates do not survive inspection.** `rank_division` has **86** values, so the top of the section-C list (gains of 55–83 divisions) spans nearly the whole bronze-1→SSL ladder, and the names are `L`, `000000`, `?`, `-`, empty string, runs of `*`, and bare first names — collisions, not climbs. Quantified with a physical-impossibility test: **18 of 39 (46 %) of the 30-day candidates show a ≥4-division rank span within a single calendar day, 11 (28 %) show ≥8**; `Wyzerer` gains 15 divisions in 0.1 days and `rokt lag` 12 in 0.0 days. Independently, across all name-weeks with ≥3 matches (4,168), **100 span ≥12 divisions inside one week** — more than the entire 39-candidate set, so the collision noise floor is larger than the signal. **Verdict: drop trajectory mining and lean on mixed-rank proxies**, exactly the branch the handoff pre-committed to. The blocker is schema, not method: `replay_players` stores only `player_name` with no platform ID, so identity cannot be resolved. **Storing the Ballchasing platform ID on ingest is the prerequisite for revisiting this.** | n/a | n/a | n/a | n/a | n/a (data sizing; no-go) |
| 23 | 2026-08-25 | **First read of the row-20 within-lobby metrics — the model orders lobbymates ABOVE chance; the `+200 MMR` rule, not the ordering, is what fails.** New `crates/rocket_league_score/examples/revalidate.rs` re-scores an existing checkpoint with frozen weights instead of re-training (~6 days, F9). It calls `ml_model_training::compute_validation_loss` directly — now `pub`, along with `ValidationLossResult` — so the numbers cannot drift from an in-training validation pass; segments are loaded **cache-only** (uncached replays are skipped and counted, so a thin cache cannot masquerade as a full eval set). Scored `lstm_v20/checkpoint_best` over the full **evaluation** split: 3,013 replays, **45,805 segments, 0 skipped**. `constant_baseline=470.8` and `lobby_mean_baseline=59.3` reproduce the epoch-255 log exactly, confirming the same validation set. **Results:** `pred_std=432.4 MMR`, `pearson_r=0.8197`. Whole-match over **2,129 lobbies**: `player=197.9 MMR`, `lobby=174.1`, **`between=161.5`, `within=115.3`**. Aggregating a player's segments to a median is worth **~135 MMR** vs the 332.6 per-segment RMSE the run was checkpointed on — per-segment RMSE has been systematically overstating the error the product actually incurs. **Within-lobby `concordance=0.553` over 5,777 pairs**, and on **mixed lobbies (gap≥150): n=175, `conc=0.619`, scored=157, `top1=34.7 %` (chance 16.7 %), `detect=3.2 %`, `margin=+18 MMR`.** Two consequences. (1) The handoff's working assumption that ordering sits at chance is **wrong**: 0.619 concordance and 2.1× chance top-1 cannot come from the lobby shortcut, because a shortcut prediction is identical across all six slots and scores exactly 0.500 / 1/6. Some per-player signal is already being read. (2) Detection is nevertheless ~0 because the mean margin is **+18 MMR against a +200 MMR threshold** — this is **F6 (no dynamic range), not F3 (no ordering)**. Re-fitting the threshold as a held-out percentile is now clearly the higher-value move than any further ordering work. | n/a | n/a | n/a | n/a | n/a (re-score, no training change) |
| 24 | 2026-08-25 | **F4 train/inference feature skew fixed and closed with regression tests; row 15 re-established.** The two extraction paths were separate loops that disagreed on three features. `feature_extractor` now has **one** implementation, `extract_player_centric_frames_with_context(frames, roster, segment_length, Option<SequenceContext>)`; `extract_player_centric_game_sequence` (training) and both inference entry points delegate to it. `SequenceContext` borrows `goals` / `goal_frames` / `kickoff_frames` off a `ParsedReplay`. **The clock features are the actual bug and are now always derived from `frame.seconds_remaining`, with or without context** — the old inference path zeroed them, producing `seconds_remaining_normalized = 0` together with `is_overtime = 0`, a pair training can never emit (a zero clock always means overtime). Goal-replay exclusion and the reconstructed score differential are enabled by context. **Every in-tree inference caller already held a `ParsedReplay` and was merely passing `&parsed.frames`**, so the skew was fully removable rather than merely gateable: new `ExtractedSegmentFeatures::from_parsed` and `predict_player_centric_per_segment_from_parsed` (sharing `predict_from_player_centric_frames` with the frame-only path), and the app (`ui/upload.rs`), `flag_smurfs`, and `smurf_spotcheck` all moved onto them. Added `SCORE_TIME_CONTEXT_FEATURE_OFFSET = 100` with a `debug_assert_eq!` at the write site. **4 regression tests**, mutation-checked by reintroducing the bug (2 of them fail as intended): training vs inference-with-context are **bit-identical** across every frame/slot/feature; goal-replay frames are provably excluded (so the equality test cannot pass vacuously); the impossible clock pair `(0, 0)` never appears on either path; and score-diff is 0 without metadata / non-zero with it. **Row 15 re-run** on the fixed path — `smurf_spotcheck --split evaluation --min-top-gap 150`, 223 held-out lobbies: **`detection_rate=0.0 %`, `top1=22.2 %` (chance 16.7 %), `mean_margin=+10 MMR`.** Above chance, consistent with row 23's direction, and again margin-limited rather than order-limited. **Unreconciled:** row 23's in-training metric reports top1 34.7 % over 157 scored lobbies vs 22.2 % over 223 here. The populations differ (`segment_top_gap_mmr` over cached segments vs `list_mixed_rank_replays` coarse-rank SQL) and coverage differs (cached segments vs whole re-parsed replays); worth reconciling before either number is quoted as *the* top-1 rate. | n/a | n/a | n/a | n/a | n/a (correctness fix + re-run) |
| 25 | 2026-08-26 | **Flag threshold re-fitted on held-out data (step 2.5) — the `+200 MMR` rule is indistinguishable from random guessing, and no threshold on this checkpoint is shippable.** New `ml_model_training::threshold` + `examples/fit_threshold.rs` fit the threshold from the margin distribution instead of asserting it, and report a **precision/recall curve** rather than a single detection rate. Two definitional changes make these numbers mean more than row 20's `mixed_detection_rate`: (a) **every player is scored, not just the top-labelled player of a mixed lobby** — that metric only ever inspects the one player who is a positive by construction, so it cannot observe a single false positive and has no precision; (b) the threshold is **fitted on one set of lobbies and reported on another**, split by replay (margins are lobby-relative, so a lobby split across both sides would compute its medians from a subset of its own players). `revalidate --dump-predictions` writes the 9,309 per-player whole-match predictions the row-20 metrics are computed from; fitting then costs milliseconds against ~1 h for a scoring pass. `threshold::mixed_lobby_cross_check` re-derives row 23's numbers from the dump and **reproduces them exactly** (`mixed=175, scored=157, top1=34.7 %, margin=+18 MMR`), so the dump faithfully represents what was measured. **Results.** The shipped rule over all 9,309 scored players: **flags 185 (1.99 %), of which 5 are correct — precision 2.7 % against a 2.43 % base rate.** It is not merely too strict; when it does fire it is right at chance. Held-out (1,079 lobbies, 4,744 players, 119 proxies, 2.51 % base rate): **average precision 0.0408 vs 0.0251 base rate — lift 1.63×**. Best held-out operating point ≈ **+32 MMR → precision 5.7 %, recall 26.1 %, F1 0.094**, while flagging 11.4 % of everyone. A percentile fitted at the base rate (+159.6) gives precision 2.9 % / recall 2.5 %. **The diagnosis, which revises F6.** F6 read the `+18 MMR` mean margin as "no dynamic range left to flag with". The range exists — margins reach `+996` — but it is **anti-informative at the top**: smurf-proxy margins have p95 `+81`, p99 `+178`, whereas everyone-else has p95 `+61`, **p99 `+455`**. The heaviest right tail belongs to the negatives, so the strictest thresholds are the *least* precise: top-1 % (+499) and top-2 % (+208) of players both score **0 true positives**. The model's largest within-lobby deviations are its largest errors, not its best detections, and the usable signal sits in modest margins (+20…+80). So the binding constraint is not the threshold *value* but the **shape** of the margin distribution, which no monotone recalibration can change. **Verdict: step 2.5 is complete and the honest answer is negative** — re-fitting converts a rule that is right at chance into one with ~2.3× lift at its best point, which is real but nowhere near shippable. Consistent with the row-23 ceiling (`conc = 0.619`), and it moves the decision squarely onto step 3. | n/a | n/a | n/a | n/a | n/a (threshold fit; no training change) |
| 26 | 2026-08-27 | **Step 3 go/no-go: GO, and the lobby context is not a shortcut the model leans on — it is actively harmful to within-lobby discrimination.** Matched pair on a fixed 3,000-replay dev subset of the **training** split (45,759 segments), 60 epochs, batch 144, `lr=3e-2`, validation on the **whole** evaluation split so both arms and the epoch-255 baseline are scored over the same 175 mixed lobbies. Arms differ in exactly one thing: `FeatureView::SelfOnly` zeroes the 79 context features (the other five cars), keeping architecture, parameter count and tensor shapes identical so the ablation cannot be confounded with a capacity change. **Control `lstm_v22_full` (full-106): mixed `conc=0.621`, `top1=28.7 %`, `detect=1.9 %`, `margin=+21 MMR`.** **Ablation `lstm_v22_self` (self-only-27): mixed `conc=0.699`, `top1=43.3 %`, `detect=16.6 %`, `margin=+82 MMR`.** Removing the lobby context **improves every ordinal metric**: concordance +0.078, top-1 +14.6 points (2.6× the 16.7 % chance rate), detection **8.7×**, and the mean margin roughly **4×**. Both arms healthy and uncollapsed (`pred_std` 431.5 vs 414.6). `pearson_r` drops 0.802 → 0.774 and RMSE-type accuracy degrades, exactly as predicted — the shortcut is worth 98.4 % of the *RMSE* objective, so losing it must cost there; that regression is the ablation working. **The handoff enumerated two outcomes and got neither.** It expected self-only either to hold near 0.619 ("signal was per-player all along") or to collapse toward 0.500 ("the edge comes from context"). Self-only instead **exceeded** the control, which means the per-player signal was not merely diluted by lobby context but **suppressed** by it: the other five cars act as a high-variance nuisance input that the shared LSTM mixes into every slot's representation. **This retro-explains row 25.** Step 2.5 found the margin distribution's heavy right tail belonged to the *negatives* (p99 `+456` for non-smurfs vs `+178` for smurfs), making the strictest thresholds the least precise. That tail is lobby-context noise: with it removed the mean margin on mixed lobbies goes `+21 → +82 MMR` and the unchanged `+200` rule fires correctly 8.7× more often. **Consequences.** (a) Step 5's two-tower design is validated, but the cheaper reading is that the skill tower should simply not see context at all — a plain self-only model already beats the full-context one on every metric that matters here. (b) **F7 dissolves**: `mixed_rank_eval` becomes a valid instrument once the tower is self-only. (c) F3's "1.6 % of label variance is within-lobby" is a statement about the RMSE objective, not about what is learnable — the ordinal signal survives fine without the 98.4 %. (d) Steps 4–8 are unblocked. **Control secondary finding:** at 11 % of the replays and 24 % of the epochs the control reaches `conc=0.621` vs the epoch-255 full-data run's `0.619` — within-lobby ordering saturates far below the full budget, so **F9's fixed dev subset is validated as the standard eval protocol** and this metric never needs a 6-day run again. | n/a | n/a | n/a | n/a | **GO** (ablation beats control on every ordinal metric) |
| 27 | 2026-08-27 | **Threshold re-fit on both step-3 arms: the self-only model turns the shipped rule from random into genuinely predictive, but the detector is still not shippable.** Ran step 2.5's `revalidate --dump-predictions` → `fit_threshold` on the matched pair, so the ablation and the threshold analysis are finally on the same footing (row 25 could only compare against `lstm_v20`, which differs in training budget as well as feature view). **Matched comparison, identical 9,309 players / 2.43 % base rate:** the shipped `+200 MMR` rule catches **6 of 185 flags (3.2 % precision) on the control** vs **33 of 408 (8.1 %) on self-only** — 5.5× the true positives and 2.5× the precision, from 1.3× the base rate to **3.3×**, without changing the rule. Held-out average precision **0.0429 → 0.0543** (lift 1.71× → **2.17×**); best held-out F1 **0.093 → 0.125**; the shipped rule on held-out goes from 2 TP / 2.2 % precision to **17 TP / 8.3 % precision at 14.3 % recall**. **The control also validates the dev subset for threshold work:** at 3,000 replays / 60 epochs it reproduces `lstm_v20`'s 27k/255-epoch numbers closely (top1 34.4 % vs 34.7 %, margin +20 vs +18, shipped precision 3.2 % vs 2.7 %, AP lift 1.71× vs 1.63×), so row 25's conclusions were not an artefact of budget and neither is this one. **Margin distribution — where the gain comes from, and what is left.** Control positives sit at p50 `+19.5`, p75 `+47.3`, p99 `+205.5`; self-only positives at p50 `+39.9`, p75 `+160.0`, p99 `+303.9`. The negatives barely move (p99 `+435.8` → `+385.5`). **So the ablation works by lifting the positives up into the tail, not by removing the negative tail** — which is why the top 1 % threshold *still* scores zero true positives on both arms, and why the extreme-margin region remains untrustworthy. **Verdict: the direction is confirmed and large, the product is not there.** At the best operating point ~92 % of flags are still wrong. Signal, not a detector. The residual obstacle is now precisely named — a heavy right tail among the negatives that survives removing lobby context — which is a target for step 6 in its "compress the tail" form (rank-index / percentile targets) rather than for any further threshold work. | n/a | n/a | n/a | n/a | n/a (threshold fit; no training change) |

## Notes

- **Rows 7–8 vs row 9 (why it “used to work”):** With **non-deterministic** Wgpu `Tensor::random` jitter, the **same CLI** could land in different basins. **Update:** label jitter is now a **pure function** of epoch + batch index + cell (`LabelJitterStep` in `crates/ml_model_training/src/minibatch_loss.rs`), so **noise is reproducible** across runs and driver versions. **Residual variance:** weight initialization and any remaining backend randomness can still depend on `Wgpu` seeding; keep `Wgpu::<f32>::seed(&device, 42)` in the harness for best parity.
- **Row 8:** After extracting shared loss, the first `--mse-only` T3 run regressed to a **constant-MMR** collapse (~976 SSL) until the ablation was aligned with eval: `forward_with_lobby_scale`, shared jitter helper, and **plain** `diff²` (not `0.5·diff²`). Full 450-epoch T3 re-validates row 7 (SSL 210.5 MMR this run).
- **`--mse-only` vs default harness:** Comparing runs for **full production behaviour** (Huber, pinball, ordinal, pairwise), **omit `--mse-only`**. The ablation still trains **spread** so it is not “pure MSE only” anymore; use it to isolate the MMR head path and jitter without the rest of the auxiliary losses.
- **Row 10:** T2 used to **NaN** around epoch 80 on Bronze/SSL mixes with the thicker production stack; ordinal **`log_sigmoid`** + logits clamp + larger spread **`ε`** addressed that. **Training** and the harness both call the same **`production_training_minibatch_loss`**.
- **T1** always passes: single SSL replay, single constant label — not a strong test of *using features*, only of the stack and optimisation.
- **T2** got *worse* in scalar RMSE vs row 1 (791 → ~1285) after changing the training signal: the old ~791 is consistent with a **global constant predictor** near the class-weighted mean; the new loss pushes the network away from that collapse but does not yet reach < 300 on **clean-target** RMSE.
- **Row 4:** Removing pinball + rank weights **helped** T2 (1285 → **1033**) and brought `pred_mean` in line with the oversampled batch mean (~1090), but RMSE is still ~**1033** — the network is **not** learning replay-specific scalars for Bronze vs SSL, only a **global level** (within-batch mean). T3 U-shape unchanged vs row 3; SSL still **1377.7**.
- **Row 5:** Higher base LR + more epochs moved T2 to **320.3** (only ~20 MMR over the 300 bar); late training (epochs 180–200) drives most of the gain. T3 / SSL barely moved vs row 4.
- **Row 6:** **`--lr-floor 0.10`** + **`--t2-epochs 270`** clears **T2** early (~epoch 139, RMSE **96**). **T3** finally moves: overall **549→432**, SSL **1388→1128** (still **FAIL** vs 350). Late T3 (epochs 200–250) shows a step down (~550→430) similar in spirit to T2’s late drop.
- **T3** SSL remains the bottleneck; row 6 is the first large SSL improvement in this table. Full per-rank tables were omitted for row 3; re-run to capture SSL and Bronze tails. Append SSL RMSE to row 3 when available.

## Proposed next steps (short list)

**Previously listed items — not all “done”:** only **T2-only ablation** and **production parity** were finished (~strikethrough items). **Open** from that list: (1) Huber-aligned **logging** in the harness, (3) more **T3 epochs / tail LR** sweeps if SSL still marginal, (4) **Lobby / jitter σ** sweeps on real training vs harness.

### T3 stability and determinism

1. **Done in code:** Mean-zero label jitter uses **`LabelJitterStep`** (no Wgpu `Tensor::random`). Production **`train()`** and harness share **`production_training_minibatch_loss`** / **`mse_ablation_minibatch_loss`** in `minibatch_loss.rs`. Row **10** validates **all tiers** on the **production** harness path with stable ordinal + spread numerics.
2. **If T3 regresses again:** tune **SSL row boost** (`--mse-only` only), **T3 learning rate**, **`LABEL_JITTER_STD`**, or **`DISTRIBUTION_SPREAD_LOSS_WEIGHT`** / pinball weights — optimisation can still have **multiple basins** even with fixed jitter.
3. **Full reproducibility hard mode:** pin **initial weights** (checkpoint from fixed seed) or audit Burn **dropout** / autotune if any non-determinism remains in forward.

Add a new table row (and bump `#`) for every new change or hyperparameter sweep so we do not re-discuss the same dead ends.
